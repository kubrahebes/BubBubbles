<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.bubbubbles">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/bubicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".AnasayfaActivity"
            android:theme="@style/anasayfa"></activity>
        <activity
            android:name=".SpleshActivity"
            android:theme="@style/anasayfa">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LetStartActivity" />
        <activity android:name=".TestScreenActivity" />
        <activity android:name=".SonucActivity"
            >

        </activity>
    </application>

</manifest>